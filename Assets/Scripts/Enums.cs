namespace TDS
{
    public enum SideType : byte
    {
        None = 0,
        Friendly = 1,
        Enemy = 2,
    }

    public enum ProjectileType : byte
    {
        None = 0,
        Òear = 1,
        Further = 2,
    }
}
